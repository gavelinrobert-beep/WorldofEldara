# Shaders Directory

This directory contains GLSL shaders used by the Henky3D rendering engine.
Shaders are loaded from this location at runtime.

The engine expects shaders to be in GLSL 4.6 core profile format.
