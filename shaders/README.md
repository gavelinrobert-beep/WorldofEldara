# Shaders Directory

This directory contains the GLSL shaders used by the Henky3D rendering engine.
These GLSL files are mirrored from `external/Henky3D/shaders/` so the game can load shaders when run from `build/bin`.

The engine expects shaders to be in GLSL 4.6 core profile format.
